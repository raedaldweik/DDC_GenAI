# Copy to .env before building. The key is inlined into the built HTML —
# use a restricted demo key only, never a production key.
VITE_ANTHROPIC_API_KEY=sk-ant-...
# Optional, defaults to claude-sonnet-5
VITE_CLAUDE_MODEL=claude-sonnet-5
